## Change
Update the header text in `src/routes/index.tsx`:
- Change the `<span>` label from **"Reference Checker"** to **"Free reference checker"**
- Change the `<h1>` from **"Check reference authenticity and citation format"** to **"Check reference authenticity and citation format for free."**

This is a two-line text edit in the page header.