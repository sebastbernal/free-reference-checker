Add the word "Free" to the header text in `src/routes/index.tsx`.

- Change the `<span>` label from **"Reference Checker"** to **"Free Reference Checker"**.
- Change the `<h1>` from **"Check reference authenticity and citation format"** to **"Free reference authenticity and citation format checker"** (or similar phrasing that naturally includes the word "Free").